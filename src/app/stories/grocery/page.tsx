import Heading from '@/components/ui/Heading';
import { FC } from 'react';

interface Props {}

const page: FC<Props> = (props): JSX.Element => {
  return <Heading title="Grocery Inventory" />;
};

export default page;
